FROM node:20-alpine

WORKDIR /app

# package.json과 package-lock.json 복사
COPY package.json package-lock.json* ./

# 의존성 설치
RUN npm install

# 포트 노출 (Vite 기본 포트는 5173이지만 vite.config.ts에서 3000으로 설정됨)
EXPOSE 3000

# 개발 서버 실행
CMD ["npm", "run", "dev"]

